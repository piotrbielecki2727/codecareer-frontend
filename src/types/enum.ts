export enum InputType {
  Text = 'text',
  Email = 'email',
  Password = 'password',
  Number = 'number',
  Tel = 'tel',
  Url = 'url',
  Search = 'search',
}

export enum FormMode {
  SignIn = 'signIn',
  SignUp = 'signUp',
}

export enum AuthPageMode {
  Candidate = 'candidate',
  Employer = 'employer',
}
